# Copia este archivo a ".env.local" y pon tu API key de Anthropic.
# NUNCA subas la key real al repositorio.
# Si no defines esta variable, la app funciona igual usando los fallbacks estáticos.
VITE_ANTHROPIC_API_KEY=
